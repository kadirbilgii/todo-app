name: Deploy to Linux Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Deploy to Linux Server
      run: |
        ssh -i ${{ secrets.SSH_PRIVATE_KEY }} kimvardev@192.168.35.16 "cd /home/kimvardev/test/todo-app && git pull origin master && npm install && npm run build"
    env:
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    

        
    
