name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: connect ssh
      run: sshpass -p ${{ secrets.PASSWORD }} ssh -tt ${{ secrets.USERNAME }}@${{ secrets.HOST }}
    - name: test
      run: ls
