name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: connect ssh
      run: ssh ${{ secrets.USERNAME }}@${{ secrets.HOST }}:${{ secrets.APP_PORT }} sshpass -p ${{ secrets.PASSWORD }}
    - name: test
      run: ls
